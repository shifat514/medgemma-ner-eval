# Copy this file to .env and fill in your real key. .env is gitignored;
# .env.example (this file, a placeholder) is committed. NEVER commit a real key.
#
# Only needed for --backend anthropic. The MedGemma runs need no key.
ANTHROPIC_API_KEY=sk-ant-xxxxx
